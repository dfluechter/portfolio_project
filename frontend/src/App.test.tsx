import { describe, it, expect } from 'vitest';

describe('App', () => {
  it('renders without crashing', () => {
    // Ein einfacher Platzhalter-Test, damit Vitest nicht abbricht
    expect(true).toBe(true);
  });
});
